---
import type { Skill } from "../../data/skills";
import { categoryColors } from "../../data/skills";

interface Props {
  skill: Skill;
}

const { skill } = Astro.props;
const colorClass = categoryColors[skill.category];
---

<span class:list={["skill-badge", colorClass]}>
  {skill.name}
</span>
